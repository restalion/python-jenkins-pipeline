#!groovy

pipeline {
    agent any
    

    stages {
        stage('Environment preparation') {
            steps {
                echo "-=- preparing project environment -=-"
                // Python dependencies
                
            }
        }    

    }
    post {
        always {
            echo "-=- remove deployment -=-"
            
        }
    }
}
